import React from 'react';
/*
const PlayersComponent = (props) => {
    let Players = props.players;
    return (
      <div>
        <h1 className="display-4 font-weight-bold text-uppercase my-4">
          List of Players
        </h1>
        <div className="playersBlock">
          {Players.map((player) => (
            <div className="card">
                <img
                src={player.photo}
                alt={player.name}
                className="image-card-top"
                />
                <div>
                      <h4 className="card-title">{player.name}</h4>
                      <p className="card-text">Total Runs: {player.runs}</p>
                </div>
            </div>
          ))}
        </div>
      </div>
    );
};


export default PlayersComponent;

*/